# Copy to ~/.config/storitad/aliases.yml to customise.
# Each alias expands to a list of real recipient ids ingest-side; the entry
# is tagged with both the alias AND the expanded ids.

aliases:
  family: [griffin, julian, casper, emma]
  boys:   [griffin, julian, casper]
